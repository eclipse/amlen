kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "{{ _amlen_name }}-pvc"
  namespace: "{{ _amlen_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ _amlen_volume_size }}"
