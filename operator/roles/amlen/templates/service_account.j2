kind: ServiceAccount
apiVersion: v1
metadata:
  name: "{{ _amlen_release }}-{{ _amlen_name }}"
  namespace: "{{ _amlen_namespace }}"
rules:
- apiGroups:
  - ""
  resources:
  - persistentVolumeClaims
  verbs:
  - list
