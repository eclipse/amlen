apiVersion: v1
kind: Service
metadata:
  name: "{{ pair_name }}-{{ node_index}}-admin"
  namespace: "{{ _amlen_namespace }}"
spec:
  selector:
    app: "{{ pair_name }}-{{ node_index }}"
  ports:
  - port: 9089 
    name: admin-port 
    
