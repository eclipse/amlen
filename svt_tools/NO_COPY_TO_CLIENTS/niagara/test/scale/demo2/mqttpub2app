server=10.10.10.10
port=16999
topic="/APP/1"

echo java svt.mqtt.mq.MqttSample -a publish -t ${topic} -s tcp://${erver}:${port} -N 10 -vv
java svt.mqtt.mq.MqttSample -a publish -t ${topic} -s tcp://${erver}:${port} -N 10 -vv
