server=10.10.10.10
port=16999
topic="/CAR/1"

echo java svt.mqtt.mq.MqttSample -a subscribe -t ${topic}  -s tcp://${server}:${port} -N 10 -vv
java svt.mqtt.mq.MqttSample -a subscribe -t ${topic}  -s tcp://${server}:${port} -N 10 -vv
