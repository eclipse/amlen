let iteration=0; while((1)); do ./ha_fail.sh; ((iteration++)); 
    echo "------------------------------------------"
    echo "current iteration is $iteration; "
    echo "------------------------------------------"
    date; 
done
